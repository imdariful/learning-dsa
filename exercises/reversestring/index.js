// --- Directions
// Given a string, return a new string with the reversed
// order of characters
// --- Examples
//   reverse('apple') === 'leppa'
//   reverse('hello') === 'olleh'
//   reverse('Greetings!') === '!sgniteerG'

/*
// ! solution 1 using build in reverse funtion
function reverse(str) {
  return str.split('').reverse().join('')

}
*/

// !solution 2
function reverse(str) {
  
}

module.exports = reverse;
